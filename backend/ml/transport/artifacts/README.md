# Trained artifacts (joblib + metrics JSON) are written by
# `python -m ml.transport.train` and `python -m ml.transport.policy_train`.
# Do not train on a traveler turn.
#
# Labels are synthetic. See ml/transport/README.md.
