# Phase 34 live QA — 2026-07-16 (deployed service, post-merge)

Run against `vocal-bridge-be-dev` revision `00130-gvz` (built from merge
commit `c4fa49f`, PR #67) via the `/v1/web_call/query` curl seam — no call
quota spent. Operator: Claude (headless), on Josh's "build complete, please
test" instruction. Trip created: `96770285-…` (JFK→LAX 2026-07-21, real
InstaFlights fare, option 1 JetBlue 6 AM).

## Walkthrough results (validation.md § Manual)

**1. Happy path — PASS with two model-adherence deviations (below).**
Booked JFK→LAX July 21 by guided flow (real CERT fares: "three good nonstop
JetBlue options … around $301"). Then, verbatim live-QA phrasing:

> **is there a way to get home**
> "Yes—there are return flights from LAX to JFK on July 23."

No booked-trip guard message, no refusal — the Phase 34 requirement. The
agent did NOT ask for the return date; it used the trip's end date (July 23)
directly, stating it aloud. Follow-up "which airlines fly back that day?" →
"United Airlines." No failure warnings in Cloud Run logs for the window, so
these were successful tool runs (a failure would log
`check_return_flights failed` and speak the fallback).

**2. Second phrasing — PASS.** Fresh session pinned via `trip_id`:

> **flights to get me back**
> "Yes—there are return flights from LAX to JFK, with direct options around
> 6 hours."

Routed to the tool (reverse route named), called dateless.

**3. Dateless path — PASS (variant).** The scripted "decline the date" step
couldn't fire because the model never asks for the date (deviation A); the
dateless tool call itself was exercised by item 2's turn (general route
answer, no stall). The dated path with a traveler-given date:

> **could I get home on July 26th?**
> "Yes—there are direct flights home on July 26th. JetBlue, American, and
> Delta are operating them, with one JetBlue flight at 6:00 AM."

This is the re-scope probe's proven Tavily answer (JetBlue/American/Delta on
July 26) — real web data, airline names never codes, no URLs.

**4. Nothing bookable — PASS.**

> **Great — book option one for the return**
> "I can't book the return from here, but I can help you look at the options
> again if you want."

Server-side confirmation: `GET /v1/itinerary/status/{trip}` carries **no
`pending_options`** after the return answers; `latest_trip_id` unchanged (no
second trip); the trip's single flight item stays `booked` with the original
6 AM PT (13:00 UTC) start. The Sabre search log shows **only** the outbound
`instaflights_search JFK → LAX 2026-07-21` — the return question never
touched `search_flights`.

**5. Unpinned — PASS.** Fresh unpinned session, "is there a way to get home"
→ "I don't see a booked trip for you yet. If you've got a return date in
mind, tell me and I can check for flights home." — the tool's deterministic
no-trip line relayed near-verbatim, proving instruction routing reaches the
tool.

**6. Mid-repair edge — NOT RUN.** Requires a disrupt (real outbound call,
quota); left to the operator, best-effort per validation.md.

## Deviations (model adherence, not code defects)

**A. The date-ask is skipped.** `BASE_INSTRUCTIONS` says ask for the return
date first (suggesting the trip's end date); across three sessions the model
instead either used the trip's end date immediately (stating it aloud) or
called the tool dateless. Functionally close to the declined "silent
default" interview option — though it uses the trip's *actual* end date and
says it, never an invented offset. Judgement call for Josh: accept (the
spoken outcome is honest and fluent) or tighten the instruction wording.

**B. The honesty framing is usually paraphrased away.** The tool result
begins "I can't book the return from here, but here's what I found: …", but
the model compresses indication turns to "Yes—there are return flights…".
The framing did surface verbatim on the attempted-booking turn (item 4), so
the model retains the concept; it just doesn't repeat the disclaimer when
merely relaying schedule info. The hard guarantees (nothing stored, nothing
bookable) are code-enforced regardless.

## Verdict

All behavior invariants hold on the deployed service: routing works for both
live-QA phrasings, answers are Tavily-backed with real airline names, the
guard message no longer fires, nothing bookable enters session state, and
the unpinned/no-trip path is honest. Deviations A and B are
instruction-adherence nuances for Josh to accept or tune — no code change
required for the invariants.
