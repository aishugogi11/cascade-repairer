# Live QA evidence — Phase 35, 2026-07-16

Run against the deployed service after PR #64 merged and Cloud Build deployed
revision `vocal-bridge-be-dev-00126-fxd` (created 2026-07-16T18:17Z, serving
100% traffic) with `TAVILY_API_KEY` confirmed present on the service. All
calls via the quota-free `/v1/web_call/query` curl seam, unique
`session_name` per call. Automated criteria were green pre-merge (532
passed, 12 skipped, 11 deselected, 7 pre-existing warnings — count verified
identical to the pre-change baseline).

## 1. Deployed live check (unpinned) — PASS

Session `p35-qa-unpinned-1846`, query: *"what is happening in Los Angeles
this weekend?"*

> "This weekend in Los Angeles, there are FIFA World Cup watch parties,
> CicLAvia, and concerts, plus outdoor movie screenings. Frogtown First
> Fridays and Sunday Afternoon Concerts at Mount Wilson Observatory are
> also on."

Real, current content; two spoken-style sentences; no URLs, no markdown.
Full turn: **9.8 s**.

## 2. Trip-aware check (pinned) — PASS

Pinned the latest trip (`9f7c0c45…`, "Trip to LAX", JFK → LAX,
2026-07-21 → 2026-07-23) via `trip_id` in the POST body. Session
`p35-qa-pinned-1847`, query: *"what should I do for fun while I am there?"*
— destination never named by the traveler.

> "For fun, try In-N-Out Burger near LAX, Griffith Observatory, and a beach
> day in Santa Monica."

The answer reflects the trip's destination from the server-side garnish.
Full turn: **6.4 s**.

## 3. Failure honesty (key unset, local) — PASS

`docker compose exec -e TAVILY_API_KEY= backend python …` calling
`destination_info_impl` directly returned exactly
`"I couldn't look that up just now — ask me again in a moment."` with no
exception and no stall; the designed `destination_info failed` warning was
logged with the RuntimeError traceback.

## 4. Latency feel — PASS

9.8 s / 6.4 s full turns (agent LLM round-trips + basic-depth Tavily),
inside the 8 s tool cap plus normal turn overhead — a natural voice pause
at demo scale. No tuning needed; revisit `max_results` only if rehearsal
feels slow.

## 5. No leakage — PASS

`GET /v1/itinerary/status/{trip_id}` before and after the destination
questions: `pending_options` absent both times, items unchanged
(`flight: fixed`). The tool wrote nothing.
